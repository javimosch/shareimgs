version: '3'

services:
  app:
    image: javimosch/shareimgs:latest
    volumes:
      - "./public/upload:/app/public/upload"
    ports:
      - "${PORT}:${PORT}"
    env_file:
      - .env
    entrypoint: node
    command: index.js